export default class Entrada {
    receberNumero(mensagem: string): number;
    receberTexto(mensagem: string): string;
    receberData(mensagem: string): Date;
}
//# sourceMappingURL=entrada.d.ts.map